/**
 * Ideally these classes would be package-private in the reflection package. But
 * this causes illegalaccessexceptions in GWT hosted mode because classes are
 * loaded from different classloaders
 */
package cc.alcina.framework.common.client.reflection.impl;