package com.google.gwt.dom.client;

/**
 * <p>
 * Ui behaviors are a wide cloud of things - such as "handle clickout of element
 * x but only in non-deskto dims".
 * 
 * <p>
 * Herding them is hard - but the first step to herding them is classifying the
 * concept of a "ui behavior" - attached to the DOM in response to some model
 * state, that itself modulates DOM events
 */
public interface EventBehavior {
}
